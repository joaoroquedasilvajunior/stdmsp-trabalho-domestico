# --- Secrets ---
# etl/.env contains the Supabase service-role key — NEVER commit
.env
.env.local
.env.*.local
*.key
*.pem

# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node (in case Cloudflare/Wrangler tooling shows up) ---
node_modules/
.wrangler/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# --- Logs / scratch ---
*.log
.scratch/

# --- ETL caches ---
# PNADC microdata zips and extracted text are huge (~200 MB per quarter,
# ~10 GB across full backfill). Always re-fetchable from IBGE FTP; never commit.
etl/raw/
metadata_probe.json

# Planilhas brutas das pesquisas — contêm CPF, nunca versionar
Condições de Trabalho para as Trabalhadoras Domésticas (Responses).xlsx
Final version May_2023.xlsx
*.xlsx

.dev.vars*
!.dev.vars.example
!.env.example
chapter/survey_b_explorer/
